# TypeScript Express Server

A simple Express server built with TypeScript. This project demonstrates how to set up an Express application with TypeScript, including user registration and authentication using bcryptjs and jsonwebtoken.

## Features

- User registration with password hashing
- User login with JWT authentication
- Protected routes
- TypeScript support
